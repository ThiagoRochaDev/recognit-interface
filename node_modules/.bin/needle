#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../grpc/node_modules/needle/bin/needle" "$@"
  ret=$?
else 
  node  "$basedir/../grpc/node_modules/needle/bin/needle" "$@"
  ret=$?
fi
exit $ret
